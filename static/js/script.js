/*const formulario = document.getElementById('form');
const cadastrarBotao = document.getElementById('cadastrar');
const aviso = document.getElementById('aviso');

formulario.addEventListener('submit', function(event) {
    event.preventDefault();

    // Simulando o cadastro com sucesso
    const cadastroSucesso = true;

    if (cadastroSucesso) {
        aviso.textContent = 'A Cultura foi cadastrada com sucesso.';
        aviso.style.display = 'block';
        formulario.reset(); // Limpar o formulário
    }
});*/

/*const formulario = document.getElementById('form');
const cadastrarBotao = document.getElementById('cadastrar');
const mensagemSucesso = document.getElementById('mensagemSucesso');
const mensagemErro = document.getElementById('mensagemErro');

formulario.addEventListener('submit', function(event) {
    event.preventDefault();

    // Simulando o cadastro com sucesso
    const cadastroSucesso = true;

    if (cadastroSucesso) {
        mensagemSucesso.style.display = 'block';
        mensagemErro.style.display = 'none';
        formulario.reset(); // Limpar o formulário
    } else {
        mensagemSucesso.style.display = 'none';
        mensagemErro.style.display = 'block';
    }
});*/

